# Repository Agent Notes

This repository includes shared Three.js skills under `.agents/skills/`.

For Three.js-related planning, implementation, or review tasks, prefer using the repo-scoped skills in `.agents/skills/` before falling back to generic guidance.
